{
  "name": "repovow-agent",
  "private": true,
  "description": "RepoVow monorepo — Rust CLI + npm distribution",
  "scripts": {
    "test": "cargo test --all-targets",
    "build": "cargo build --release",
    "stage": "./scripts/stage-npm.sh",
    "release": "./scripts/release.sh",
    "install:global": "./scripts/release.sh --install-global",
    "verify:shim": "node npm/repovow-cli/scripts/verify-shim.js"
  }
}
